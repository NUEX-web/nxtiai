export {};
// Stray duplicate file accidentally created at repo root during a GitHub-web-upload deployment on 2026-09-09.
// The real file lives at its correct nested path; this root-level copy is unused by the app.
// Safe to delete -- neutralized here (rather than left with broken content) to unblock the Vercel build.
